# 神秘 Agent 的 PRD

> [!warning]
> A Markdown editor or reader that fully supports GFM (GitHub Flavoured Markdown) is strongly recommended.

> Reversion 2, 2026/01/30, 12: 24
>
> Author: WASDDestroy
>
> Title: 神秘 Agent 的 PRD
>
> VISUAL STUDIO CODE IS THE BEST TEXT EDITOR EVER!

## 引言

在当下的开发中，AI辅助生成代码已经成为不可或缺的一部分。但是在实际使用中我们注意到一些问题：

1. Agent 模式下的 AI 容易出现注意力分散问题，本应当用于理解代码功能并针对性提出解决方案的算力被浪费在使用*正确的工具（例如不应当在 Windows 平台使用 UNIX/UNIX Like 命令）*上。且在注意力分散的情况下 AI 很容易使用

2. “阅读”代码需要大量的 `token` ，就算是较为经济的国内某模型 v3.2 也无法满足高规格的开发和重构。在项目代码量为几千行左右的中等项目中这样的问题就已经非常明显。

3. API 调用的 AI 模型的编码和推理性能不如网页版本。

4. 部分已有的 AI Agent 客户端，例如 `Continue`，提供的工具链很难正确套用代码 patch ，部分极端情况下甚至不能正确列出项目目录中的文件。

## 目的

创建一个能够提供下面功能的 AI Agent 客户端：

> [!note]
> 为了理解方便，先简单介绍架构：
>
> 1. Client: 提供基础的 MCP 和自动工具，例如 `read`, `list`, `patch`, `prompt`等，方便读取、写入和 patch 代码；并提供网络接口和无头浏览器方便访问用于代码总结和问题分析的 AI 模型。
>
> 2. AI Model 1 - Analyser: 用于分析代码用途并决断是 `抽象` 还是 `暴露` 源码。可以使用 API 访问，也可以使用无头浏览器打开网页端模型。根据用户需求决定是否有必要将某个函数或者类的实现暴露给通过无头浏览器驱动的网页 AI 模型。
>
>    - 为了节省 token，应当设计一些自动化工具，初步处理一些常见方法的逻辑。这些代码就不用暴露给 Analyser 了。
>
> 3. AI Model 2 - Coder: 在无头浏览器运行的网页 AI 模型。用于分析问题，提供编码，或者执行重构，etc.

1. 稳定易用的 MCP 和自动化工具，方便和 AI 模型进行交互。

2. 提供 token 经济的代码分析方案。

3. 基于代码分析功能提供可选的网页端模型服务，实现问题分析、代码编写和重构任务。

**I personally call it Copliot 2.0.**

## 目标受众

1. 初学工程化开发或者正在转语言的开发者和学生群体，为 ta 们提供**精确且经济的代码分析和建议**，乃至于“AI 亲自上手改代码”。

2. 有较高经济要求的**中大型项目**开发者。

> [!note]
> 以上用户画像的重点在于经济需求。
>
> 国外的先进模型大多消耗大量 token ，就算是经济模型也无法避免在大型项目陷入炸 token 上限的泥沼。我们希望通过前期的自动工具和经济的 Analyser 提供精确的代码理解，减少 Coder 模型的重复和错误劳作。**（需求、痛点、动机）**

## 特性与功能

1. 必须有：

    - MCP 和自动化工具，提供基础的 `文件操作` 、 `命令执行` 、 `基本代码分析` 服务。

    - 基于 API 或者 Web 浏览器 的 AI Analyser， **提供比单独的 Agent 模型更优的分析能力**

    - 基于 脚本 和 Analyser决定向用户和 Coder 进行 `抽象` 和 `暴露` 的源代码

2. 应该有：
    
    - 基于 Web 浏览器 的 AI Coder

    - 根据用户附加提示词推断用户需求：分析、实现、还是重构？

3. 可以有：

    - 连接到本地部署的模型。

    - 非 CLI 的 **超美丽用户界面**，可以考虑 `WebUI`。

    - 作为扩展集成进主流IDE, 例如 `VS Code` 和 `JetBrains 系列`。

    - 允许用户使用方便的脚本语言（建议 `Python`）制作模型的 `扩展` 并决定扩展的作用域（Analyser or Coder or BOTH?），提升模型能力。

4. 本次不做：

    - 超高性能客户端。重点是保障功能，且性能瓶颈在于云端 AI。

## 用户流程：

我不太会做流程图，烦请看看下面的神奇 `lambda 表达式`： ~~我只是想敲这个箭头了: -> ~~：

用户启动 Client -> 提供可选的提示词（以及在配置文件中存储的预设提示词，鉴于至少需要向 Analyser 暴露基本工具，因此预设提示词一定存在） -> Analyser 工作，将结果返回到客户端。

还有神奇代码段：

while (用户继续操作吗？) {

    if (用户不希望模型介入代码编写) {
        
        将分析结果存储到本地文件中，减少下次调用的开销。

        结果上屏，并提供用户友好的输出。

    } else {
        
        分析结果上屏，并明确提示正在进行编写任务。

        将分析结果和要求整合成新的提示词，发送给 Coder。

        Coder 将工作的结果传送给客户端，客户端套用补丁（如果最终做了 IDE 扩展，应当在编辑区添加“采纳/拒绝”按钮）。

    }

}

## 系统与技术要求

1. 性能基准：不过度要求性能不代表着彻底不要求性能：

    - 建议客户端冷加载时间：不超过2s，且提供加载动画。

    - 建议存储空间占用：200MiB及以下。

    - 建议使用成熟**易用和高效**的数据库系统管理缓存的分析结果。

2. 开发规范：我真的觉得我像是搞软工的：

    - 统一使用**GitHub 上的团队仓库**，使用 `Git` 进行分布式开发。

    - 不建议直接推送 `main分支` ，推荐将功能、修复、重构等推送到不同分支后合并。

    - **建议**贯彻 `WaE原则` （Warnings as Errors）。

    - 提供充足的**可以在 Production Build 中被关闭的**日志。

    - 避免取巧的写法，增强代码**健壮性**。

    - 提供**低耦合度、便于协作**的代码。项目可能需要使用不同语言进行开发，请务必做到这一点。

    > [!note]
    > 理论上你只需要知道下面几个 Git 命令就行了：
    >
    > '''shell
    >
    > git clone
    >
    > git pull
    >
    > git checkout -b <new_branch_name>
    >
    > git add <a_file_name>
    >
    > git commit -m <your_message>
    >
    > git push
    >
    > '''
    >
    > 或者使用图形化 Git 客户端。

3. 安全标准：

    - 不允许 AI 和恶意网络请求访问到本地暴露的工具之外的系统资源。

    - 网络传输应当是安全和健壮的。

    - 不应当执行包含恶意代码的用户扩展。

4. 可扩展性需求：

    - 允许用户使用 Python 等脚本语言创建扩展，增加模型功能。

    - 项目本身应当是低耦合的。

5. 合规需求：

    - 无。

## 假设与约束条件

1. 我们假设用户提供的提示词本身不需要优化，例如不使用过于口语的表达。

    > [!note]
    > 多数模型现在不需要用户在提示词中指定身份了。但是保险起见我们在预设提示词中应当指定。

## 成功指标和发布标准

1. 根据**系统与技术需求**中的要求完成**特性与功能**中**必须有**和**应该有**的部分并通过整体测试和稳定性测试，有一定实用价值，则为 `成功`。

2. 完成**必须有**部分并通过整体测试和稳定性测试，则可以 `发布`。

~~3. 获奖，则为 `大成功`。~~

## 时间表与发布计划

1. 1月内：召开线上会议，讨论：
    
    - 功能的可行性和必要性

    - 是否存在其他需要的功能或者规划中不应当被加入的功能

    - 成员分工，各自的时间安排

    - 具体的开发规范，例如命名规则，是否执行WaE等

2. 2月：完成客户端的命令行实现，初步调通客户端到 Analyser 的全流程。

3. 3月上旬（待确定）：添加**必须有**的其他功能，开始做**应该有**的部分。进行稳定性测试。

4. 3月下旬（待确定）：*建议*做**可以有**的部分 / 收尾工作，提高系统稳定性，制作生产发行版和宣传物料。

6. 4月：完成未按照计划执行的任务。~~4月是你的谎言~~